import "tfplan/v2" as tfplan

# Disallowed roles
disallowed_roles = [
    "roles/owner",
    "roles/editor"
]

# Main rule
main = rule {
    all tfplan.resources.google_project_iam_member as _, iam {
        iam.change.after.role not in disallowed_roles
    }
}